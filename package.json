{
  "name": "btt-window-manager-preset",
  "author": "Elvin D",
  "license": "MIT",
  "scripts": {
    "start": "vite dev",
    "build": "vite build && mv dist/index.html ./WindowManager.html",
    "build:prod": "[ -f .env.local ] && mv .env.local .env.local.tmp; pnpm build; [ -f .env.local.tmp ] && mv .env.local.tmp .env.local"
  },
  "devDependencies": {
    "eslint-config-elv1n-react": "^1.3.1",
    "typescript": "^4.8.4",
    "vite": "^3.1.6",
    "vite-plugin-singlefile": "^0.12.2"
  },
  "eslintConfig": {
    "extends": ["elv1n-react"]
  }
}
